# Server-side only. Never put real secrets in frontend files or commit them.
OPENAI_API_KEY=
OPENAI_MODEL=
OPENAI_TIMEOUT_MS=30000
# Optional proxy-compatible endpoint:
# OPENAI_BASE_URL=https://api.openai.com/v1/responses
